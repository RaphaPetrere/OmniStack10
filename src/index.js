import React from 'react';
import ReactDOM from 'react-dom';

import App from './App'; //importando o App.js

ReactDOM.render(<App />, document.getElementById('root'));
//todo lugar q tiver tag de abertura e fechadura tipo HTML, ce importa o REACT
//ReactDOM da a habilidade do REACT de se comunicar com a arvore de elementos
//ta mandando o reactDOM renderizar dentro da DIV com id root

